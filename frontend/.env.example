# Copy to .env.local and fill in. .env.local is gitignored.

# URL of the FastAPI backend. Defaults to localhost for dev.
# In production, set this to your Railway deployment URL (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:8000
